{
  "name": "crudjt",
  "version": "1.0.1",
  "description": "Fast B-tree–backed token store for stateful sessions",
  "main": "index.js",
  "scripts": {
    "cargo-build": "cargo build --release --message-format=json-render-diagnostics > cargo.log",
    "install": "npm run cargo-build && npx neon dist < cargo.log",
    "debug": "cargo build --message-format=json-render-diagnostics && npx neon dist < cargo.log",
    "test": "cargo test"
  },
  "author": "Vlad Akymov (v_akymov)",
  "license": "MIT",
  "dependencies": {
    "@grpc/grpc-js": "^1.14.3",
    "@grpc/proto-loader": "^0.8.0",
    "lru-cache": "^11.0.1",
    "msgpack-lite": "^0.1.26",
    "@neon-rs/cli": "0.1.73"
  },
  "keywords": [
    "token",
    "session",
    "authentication",
    "authorization"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/crudjt/crudjt-javascript"
  },
  "engines": {
    "node": ">=18"
  }
}
